# Development dependencies

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0



flake8>=5.0.0
# Pin black to the same version pre-commit uses so the CI "black --check" step
# and the pre-commit hook can never disagree (unpinned black drifts on new releases).
black==25.1.0
isort>=5.10.0
ruff
pre-commit>=3.0.0

# Type checking
mypy>=1.0.0
types-requests>=2.32.0
types-urllib3>=1.26.0

# Documentation
mkdocs>=1.4.0
mkdocs-material>=8.5.0

# Include optional dependencies for testing
-r requirements.txt
