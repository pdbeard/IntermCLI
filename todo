refactor check_dependency function in tools into ehancement_loader, so you can just pass a dep map into a oneliner.

This will also require moving the dep_map (desciption, package to try) and install_map (package to try, actual package name to install ) into a seperate config file for easy changes.

Need both dep map and install map because of packages like pyyaml. Where the package is pyyaml but the package import name is yaml.

tool_name is used multiple times and can be refactored

Clear path from the tool manifest (or another config file) into the tools. Needs a simple map of of the depency required, a description, and off to the races.


Make rich text support more consistent color mapping and styling (add to 2.0 release after coming up with new styles)
 - ie, green for "available", or successful messages
 - colors for path (purply)
